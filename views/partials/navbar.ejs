<nav>
    <div>
        <a href="/">Homepage</a>
        <a href="/coffeeshops">Favorites</a>
    </div>
    <div class="logo">CoffeeMe</div>
    <div>
        <% if(user) { %>
            <span><%= user.username %></span>
            <a href="/auth/logout">Log Out</a>    
        <% } else { %>
            <a href="/auth/login">Log In</a>
            <a href="/auth/new">Sign up</a>
        <% } %>
    </div>
 </nav>